name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Create Firebase credentials file
      run: |
        mkdir -p Backend/Clinic/Firebase
        echo '${{ secrets.FIREBASE_CREDENTIALS }}' > Backend/Clinic/Firebase/clinic-firebase.json

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'

    - name: Restore dependencies
      run: dotnet restore Backend/Clinic.sln

    - name: Build
      run: dotnet build Backend/Clinic.sln -c Release --no-restore

    - name: Login to Docker Hub
      if: github.ref == 'refs/heads/main'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and Push Docker Image
      if: github.ref == 'refs/heads/main'
      uses: docker/build-push-action@v5
      with:
        context: ./Backend
        push: true
        tags: |
          ${{ secrets.DOCKERHUB_USERNAME }}/clinic-api:latest
          ${{ secrets.DOCKERHUB_USERNAME }}/clinic-api:${{ github.sha }}
